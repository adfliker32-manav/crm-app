require('dotenv').config(); const mongoose = require('mongoose'); async function run() { await mongoose.connect(process.env.MONGO_URI); const IntegrationConfig = require('./src/models/IntegrationConfig'); const config = await IntegrationConfig.findOne({ userId: '69f05bacd79a6855c5e80dc0' }).select('+email.emailUser +email.emailPassword'); console.log(config ? 'Found config, emailUser: ' + config.email?.emailUser : 'Not found'); mongoose.disconnect(); } run().catch(console.error);
