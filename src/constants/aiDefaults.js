// Shown in the UI as "Adfliker Advance". Existing tenants keep their persisted choice; see scripts/migrate-ai-default-model.js.
module.exports = Object.freeze({
    DEFAULT_AI_PROVIDER: 'openai',
    DEFAULT_AI_MODEL: 'gpt-4o-mini'
});
